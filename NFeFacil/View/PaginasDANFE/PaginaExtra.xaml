<UserControl x:Class="NFeFacil.View.PaginasDANFE.PaginaExtra"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:partes="using:NFeFacil.View.PartesDANFE"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="ms-appx:///View/PartesDANFE/EstilosPadrao.xaml" />
    </UserControl.Resources>
    <Grid x:Name="grd"
          Width="{x:Bind LarguraPagina}"
          Height="{x:Bind AlturaPagina}">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="espacoParaProdutos" />
            <RowDefinition Height="Auto"
                           x:Name="espacoParaInfo" />
        </Grid.RowDefinitions>
        <partes:CampoProdutos x:Name="campoProdutos" />
        <StackPanel x:Name="geralCampoInfo"
                    Padding="{StaticResource PaddingPadrao}"
                    Grid.Row="1"
                    BorderThickness="1"
                    BorderBrush="Black"
                    CornerRadius="2">
            <Border BorderThickness="0,0,0,1"
                    BorderBrush="Black">
                <TextBlock>CONTINUAÇÃO INFORMAÇÕES COMPLEMENTARES</TextBlock>
            </Border>
            <RichTextBlockOverflow x:Name="campoInfo" />
        </StackPanel>
    </Grid>
</UserControl>
