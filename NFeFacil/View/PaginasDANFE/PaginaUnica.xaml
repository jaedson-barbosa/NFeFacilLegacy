<UserControl x:Class="NFeFacil.View.PaginasDANFE.PaginaUnica"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:partes="using:NFeFacil.View.PartesDANFE"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <Grid Width="{x:Bind LarguraPagina}"
          Height="{x:Bind AlturaPagina}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Padding="{x:Bind MargemPadrao}">
            <partes:Canhoto DataContext="{x:Bind ContextoCanhoto}" />
            <partes:DadosNFe />
            <partes:CampoCliente />
            <partes:CampoImposto />
            <partes:CampoTransporte />
        </StackPanel>
        <StackPanel Padding="{x:Bind MargemPadrao}" Grid.Row="2">
            <partes:CampoISSQN />
        </StackPanel>
    </Grid>
</UserControl>
