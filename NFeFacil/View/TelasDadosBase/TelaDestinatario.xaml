<UserControl x:Class="NFeFacil.View.TelasDadosBase.TelaDestinatario"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///View/Estilos/Estilos.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <StackPanel>
            <ItemsControl Style="{StaticResource ItensResponsivos}">
                <TextBox Text="{Binding Cliente.nome, Mode=TwoWay}"
                         InputScope="Text"
                         Header="Nome/Razão social *" />
                <ComboBox SelectedIndex="{Binding TipoDocumento, Mode=TwoWay}"
                          Header="Tipo">
                    <ComboBoxItem Content="CPF" />
                    <ComboBoxItem Content="CNPJ" />
                    <ComboBoxItem Content="ID Estrangeiro" />
                </ComboBox>
                <TextBox Text="{Binding Documento, Mode=TwoWay}"
                         InputScope="Number"
                         Header="Documento *" />
                <CheckBox Content="Isento do ICMS"
                          Checked="chkIsento_Checked"
                          Unchecked="chkIsento_Unchecked" />
                <TextBox x:Name="txtIE"
                         Text="{Binding Cliente.inscricaoEstadual, Mode=TwoWay}"
                         InputScope="Number"
                         Header="Inscrição estadual" />
                <TextBox Text="{Binding Cliente.ISUF, Mode=TwoWay}"
                         InputScope="Number"
                         Header="Inscrição na SUFRAMA" />
                <TextBox Text="{Binding Cliente.email, Mode=TwoWay}"
                         InputScope="EmailNameOrAddress"
                         Header="Email" />
            </ItemsControl>
            <TextBlock Text="Endereço"
                       Style="{StaticResource TitleTextBlockStyle}" />
            <ItemsControl Style="{StaticResource ItensResponsivos}">
                <CheckBox Content="Nacional"
                          IsChecked="{Binding Nacional}"
                          Checked="rdbNacional_Checked"
                          Unchecked="rdbNacional_Unchecked" />
                <ComboBox x:Name="cmbUF"
                          ItemsSource="{Binding Path=UFs, Mode=OneWay}"
                          SelectedItem="{Binding Path=UFEscolhida, Mode=TwoWay}"
                          Header="Estado" />
                <ComboBox x:Name="cmbMunicípio"
                          ItemsSource="{Binding Municipios}"
                          SelectedItem="{Binding Path=MunicipioEscolhido, Mode=TwoWay}"
                          Header="Município" />
                <TextBox Text="{Binding Path=Cliente.endereço.logradouro, Mode=TwoWay}"
                         InputScope="Text"
                         Header="Logradouro *" />
                <TextBox Text="{Binding Path=Cliente.endereço.numero, Mode=TwoWay}"
                         InputScope="Text"
                         Header="Número *" />
                <TextBox Text="{Binding Path=Cliente.endereço.complemento, Mode=TwoWay}"
                         InputScope="Text"
                         Header="Complemento" />
                <TextBox Text="{Binding Path=Cliente.endereço.bairro, Mode=TwoWay}"
                         InputScope="Text"
                         Header="Bairro *" />
                <TextBox x:Name="txtCEP"
                         Text="{Binding Path=Cliente.endereço.CEP, Mode=TwoWay}"
                         InputScope="Number"
                         Header="CEP" />
                <TextBox Text="{Binding Path=Cliente.endereço.telefone, Mode=TwoWay}"
                         InputScope="Number"
                         Header="Telefone" />
                <TextBox x:Name="txtNomePaís"
                         Text="{Binding Path=Cliente.endereço.xPais, Mode=TwoWay}"
                         InputScope="Text"
                         Header="Nome do país" />
                <TextBox x:Name="txtCódigoPaís"
                         Text="{Binding Path=Cliente.endereço.cPais, Mode=TwoWay}"
                         InputScope="Number"
                         Header="Codigo do país" />
                <HyperlinkButton Content="Códigos"
                                 NavigateUri="http://www.lgncontabil.com.br/icms/Tabela-Codigo-de-Pais-do-BACEN.pdf" />
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
