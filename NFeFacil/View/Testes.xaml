<Page x:Class="NFeFacil.View.Testes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:NFeFacil.View"
      xmlns:VM="using:NFeFacil.ViewModel"
      xmlns:Conversores="using:NFeFacil.ViewModel.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <VM:DadosEstadosParaView x:Key="estados" />
            <Conversores:EstadoToMunicipios x:Key="obterEstados" />
        </ResourceDictionary>
    </Page.Resources>
    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ComboBox ItemsSource="{Binding Source={StaticResource estados}, Path=EstadosCompletos}"
                  SelectedItem="{Binding Valor, Mode=TwoWay}"
                  DisplayMemberPath="Nome"/>
        <ComboBox ItemsSource="{Binding Valor, Converter={StaticResource obterEstados}}"
                  SelectedValue="{Binding Mun, Mode=TwoWay}"
                  SelectedValuePath="Nome"
                  DisplayMemberPath="Nome"/>
    </StackPanel>
</Page>
