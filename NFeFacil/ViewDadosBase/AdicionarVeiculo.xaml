<ContentDialog x:Class="NFeFacil.ViewDadosBase.AdicionarVeiculo"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:VM="using:NFeFacil.ViewModel"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Title="Adicionar veiculo"
               PrimaryButtonText="Concluir"
               SecondaryButtonText="Cancelar"
               PrimaryButtonClick="ContentDialog_PrimaryButtonClick">
    <StackPanel>
        <TextBox Header="Descrição breve"
                 TextWrapping="Wrap"
                 Text="{x:Bind Item.Descricao, Mode=TwoWay}" />
        <ComboBox ItemsSource="{x:Bind VM:DadosEstadosParaView.EstadosCompletos}"
                  SelectedValue="{x:Bind Item.UF, Mode=TwoWay}"
                  Header="Estado"
                  DisplayMemberPath="Nome"
                  SelectedValuePath="Sigla"
                  HorizontalAlignment="Stretch" />
        <TextBox Header="Placa"
                 Text="{x:Bind Item.Placa, Mode=TwoWay}" />
        <TextBox Header="RNTC"
                 Text="{x:Bind Item.RNTC, Mode=TwoWay}" />
    </StackPanel>
</ContentDialog>
