<ContentDialog x:Class="NFeFacil.Produto.CaixasDialogoProduto.AddDeclaracaoExportacaoDireta"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:controles="using:BaseGeral.Controles"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Title="Declaração export. direta"
               PrimaryButtonText="Adicionar"
               SecondaryButtonText="Cancelar">

    <controles:EntradaRestrita Pattern="[^\d]"
                               Text="{x:Bind Declaracao.NDraw, Mode=TwoWay}"
                               Header="Número do ato concessionário de Drawback" />

</ContentDialog>
