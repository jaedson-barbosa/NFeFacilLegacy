<ContentDialog x:Class="NFeFacil.Produto.GerenciamentoProdutos.AdicionarAlteracaoEstoque"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:controles="using:BaseGeral.Controles"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Title="Alteração manual"
               PrimaryButtonText="Alterar"
               SecondaryButtonText="Cancelar">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <RadioButton IsChecked="{x:Bind Positivo, Mode=TwoWay}">Adicionar quantidade</RadioButton>
        <RadioButton Grid.Row="1">Remover quantidade</RadioButton>
        <controles:EntradaNumerica Grid.Row="2"
                                   Format="2"
                                   Number="{x:Bind Valor, Mode=TwoWay}" />
    </Grid>
</ContentDialog>
